<!DOCTYPE html>
<html lang="en">
    <head>
        <title>Employee</title>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
    </head>
    <body>
        <% mang.forEach(e => { %>
            <h1><%= e.name %></h1>
            <p><%= e.age %></p>
        <% }); %>
    </body>
</html>